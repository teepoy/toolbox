experiment:
  img_sizes: [64, 128]
  dropped_img_sizes: [128]
  notes: "Deprecated: 128 dropped for ConvNeXtV2-Atto due to stem/neck stride behavior."
  shots: [10, 20, 50]
  seeds: [0]
  families: [official, random, byol, mocov3, supcon, dino, yolo26n, svm]

pretrain:
  epochs: 10

train:
  epochs: 20
