experiment:
  shots: [1, 5, 10, 20, 50]
  seeds: [0, 1, 2]
  families: [official, random, byol, mocov3, supcon, dino, yolo26n, svm]

pretrain:
  epochs: 60
  early_stop_enabled: true
  early_stop_min_epochs: 10
  early_stop_patience: 5
  early_stop_min_delta: 0.005

train:
  epochs: 20

data:
  ssl_pool_per_class: 200
