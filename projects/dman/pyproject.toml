[build-system]
requires = ["maturin>=1.4,<2.0"]
build-backend = "maturin"

[project]
name = "dman"
requires-python = ">=3.8"
version = "0.1.0"
description = "dman Python bindings"

[tool.maturin]
features = ["pyo3/extension-module"]
manifest-path = "crates/python/Cargo.toml"
